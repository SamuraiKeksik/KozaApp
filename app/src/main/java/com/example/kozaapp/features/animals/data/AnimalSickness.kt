package com.example.kozaapp.features.animals.data

import androidx.room.Entity
import androidx.room.ForeignKey

@Entity(
    tableName = "animal_sicknesses",
    foreignKeys = {@ForeignKey(
        entity =
    )}

    )
class AnimalSickness {
}